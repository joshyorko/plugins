interface:
  display_name: "RCC Robots"
  short_description: "RCC robot.yaml projects, environments, artifact use, bundles, templates, and validation."
  default_prompt: "Use $rcc-robots for robot.yaml automation projects, conda/uv environments, freezes, Environment Artifact use, bundles, templates, and task runtime debugging."
